---
phase: 07-native-linux-build-with-skse-tcp-relay
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Code/relay_dll/xmake.lua
  - Code/native_client/xmake.lua
  - xmake.lua
autonomous: true
requirements: [RELAY-09]
must_haves:
  truths:
    - "XMake configures and builds a minimal MinGW DLL target with only MinHook and winsock2"
    - "XMake configures and builds a native Linux ELF binary target linking EnTT, spdlog, TiltedCore, encoding"
    - "Both targets coexist in the build system without conflicts"
  artifacts:
    - path: "Code/relay_dll/xmake.lua"
      provides: "Build target for skyrim_coop_hooks.dll (MinGW only)"
      contains: "target(\"SkyrimCoopHooksDLL\")"
    - path: "Code/native_client/xmake.lua"
      provides: "Build target for skyrim-coop native ELF binary"
      contains: "target(\"SkyrimCoopNative\")"
  key_links:
    - from: "xmake.lua"
      to: "Code/relay_dll/xmake.lua"
      via: "includes directive"
      pattern: "includes.*relay_dll"
    - from: "xmake.lua"
      to: "Code/native_client/xmake.lua"
      via: "includes directive"
      pattern: "includes.*native_client"
---

<objective>
Create XMake build targets for the two new binaries: the minimal MinGW relay DLL and the native Linux ELF client.

Purpose: Per D-12 and D-13, the architecture splits into two separate build targets with very different dependency sets. The DLL target uses only MinHook + winsock2 (MinGW). The native target links the full stack natively. These targets must coexist with existing targets.

Output: Two new xmake.lua files (relay_dll, native_client) and root xmake.lua includes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-native-linux-build-with-skse-tcp-relay/07-CONTEXT.md
@.planning/phases/07-native-linux-build-with-skse-tcp-relay/07-RESEARCH.md
@Code/client/xmake.lua
@xmake.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create relay DLL XMake target (MinGW only)</name>
  <files>Code/relay_dll/xmake.lua</files>
  <read_first>
    - Code/client/xmake.lua (existing client target structure, especially SkyrimTogetherClientDLL section)
    - xmake.lua (root build config, package declarations, platform guards)
    - .planning/phases/07-native-linux-build-with-skse-tcp-relay/07-CONTEXT.md (D-10, D-11, D-13)
  </read_first>
  <action>
    Create `Code/relay_dll/xmake.lua` defining a `SkyrimCoopHooksDLL` target.

    Target properties per D-13:
    - `set_kind("shared")` — produces a DLL
    - `set_group("RelayDLL")` — separate build group
    - `set_basename("skyrim_coop_hooks")` — output: skyrim_coop_hooks.dll
    - `set_prefixname("")` — no "lib" prefix on MinGW
    - Platform guard: `if is_plat("mingw") then ... end` — this target only builds under MinGW
    - Source files: all `.cpp` files in `Code/relay_dll/` (relay_main.cpp, tcp_server.cpp, hook_trampolines.cpp, command_queue.cpp, process_launcher.cpp will be added in Plan 03)
    - Include dirs: `Code/relay_dll/` (for protocol.h, command_queue.h)
    - Packages: `minhook` only (already declared in root xmake.lua)
    - System links: `ws2_32` (winsock2), `kernel32`
    - NO EnTT, NO TiltedCore, NO spdlog, NO encoding library (per D-13)
    - Static link MinGW runtime: `-static-libgcc`, `-static-libstdc++`, `-Wl,-Bstatic -lstdc++ -lpthread -Wl,-Bdynamic`
    - Force-include MinGWCompat.h for MSVC compat: `add_cxflags("-include MinGWCompat.h", {force = true})`
    - Add `Code/client/Games/Skyrim` to include dirs for Address Library/POINTER_SKYRIMSE resolution (DLL needs these for hook installation)
    - Add the existing `SkyrimCoopHooks` and `SkyrimCoopReverse` deps for address resolution

    Note: Source files beyond protocol.h and command_queue.cpp don't exist yet -- they'll be created in Plan 03. The xmake.lua should use `add_files("*.cpp")` glob so new files are picked up automatically. For now, create a minimal `relay_main.cpp` stub so the target has at least one compilable file:
    ```cpp
    // Code/relay_dll/relay_main.cpp -- stub, replaced in Plan 03
    #include "protocol.h"
    ```
  </action>
  <verify>
    <automated>cd /media/bighass/4d2c4781-34de-41a5-8939-e35551bc9a5d5/Projects/SkyrimCoop && xmake config -p mingw -m releasedbg -y 2>&1 | tail -5 && xmake build SkyrimCoopHooksDLL 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Code/relay_dll/xmake.lua exists
    - Code/relay_dll/xmake.lua contains `target("SkyrimCoopHooksDLL")`
    - Code/relay_dll/xmake.lua contains `set_kind("shared")`
    - Code/relay_dll/xmake.lua contains `set_basename("skyrim_coop_hooks")`
    - Code/relay_dll/xmake.lua contains `add_syslinks("ws2_32"` or `add_syslinks("ws2_32",`
    - Code/relay_dll/xmake.lua contains `add_packages("minhook")`
    - Code/relay_dll/xmake.lua does NOT contain `entt` or `spdlog` or `TiltedCore`
    - Code/relay_dll/xmake.lua is wrapped in `if is_plat("mingw")`
    - `xmake config -p mingw` succeeds without errors
  </acceptance_criteria>
  <done>Relay DLL XMake target configures and compiles a minimal DLL with only MinHook and winsock2 dependencies. No heavy libraries linked.</done>
</task>

<task type="auto">
  <name>Task 2: Create native client XMake target and wire both into root</name>
  <files>Code/native_client/xmake.lua, Code/native_client/main.cpp, xmake.lua</files>
  <read_first>
    - xmake.lua (root config, all includes(), package declarations)
    - Code/client/xmake.lua (existing client target for reference on deps)
    - .planning/phases/07-native-linux-build-with-skse-tcp-relay/07-CONTEXT.md (D-12, D-14)
  </read_first>
  <action>
    **Create `Code/native_client/` directory and xmake.lua.**

    Target properties per D-12:
    - `target("SkyrimCoopNative")`
    - `set_kind("binary")` — native ELF executable
    - `set_group("NativeClient")`
    - `set_basename("skyrim-coop")` — output: skyrim-coop
    - Platform guard: `if is_plat("linux") then ... end` — builds only on native Linux
    - Source files: `add_files("**.cpp")` for all native_client source
    - Include dirs: `Code/native_client/`, `Code/relay_dll/` (for shared protocol.h), `Code/client/Games/Skyrim/` (for game struct headers per D-14)
    - Dependencies: `SkyrimEncoding`, `SkyrimTogetherServer` (for embedded server in host mode)
    - Packages: `entt`, `spdlog`, `glm`, `rpmalloc`, `hopscotch-map`, `cryptopp`, `enet6`
    - System links: `pthread` (for ptrace, pread)
    - Compiler flags: `-mms-bitfields` (match MSVC struct layout for game struct headers per Pitfall 6)
    - Language: C++20

    **Create minimal `Code/native_client/main.cpp` stub:**
    ```cpp
    #include <cstdio>
    #include <cstdlib>
    // Code/native_client/main.cpp -- entry point, replaced in Plan 04
    int main(int argc, char* argv[]) {
        if (argc < 3) {
            fprintf(stderr, "Usage: skyrim-coop --pid <pid> --port <port>\n");
            return 1;
        }
        printf("skyrim-coop native client stub\n");
        return 0;
    }
    ```

    **Update root `xmake.lua`:** Add `includes("Code/relay_dll")` and `includes("Code/native_client")` near the existing `includes("Code/client")` line. Ensure the includes are placed after all package declarations so the new targets can reference them. The native_client include should be unconditional (it has its own internal platform guard). The relay_dll include should also be unconditional (it has its own internal `if is_plat("mingw")` guard).
  </action>
  <verify>
    <automated>cd /media/bighass/4d2c4781-34de-41a5-8939-e35551bc9a5d5/Projects/SkyrimCoop && xmake config -p linux -m release -y 2>&1 | tail -5 && xmake build SkyrimCoopNative 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Code/native_client/xmake.lua exists and contains `target("SkyrimCoopNative")`
    - Code/native_client/xmake.lua contains `set_kind("binary")`
    - Code/native_client/xmake.lua contains `add_packages("entt"`
    - Code/native_client/xmake.lua contains `add_packages("spdlog"`
    - Code/native_client/xmake.lua contains `-mms-bitfields`
    - Code/native_client/main.cpp exists and contains `int main(`
    - xmake.lua (root) contains `includes("Code/relay_dll")`
    - xmake.lua (root) contains `includes("Code/native_client")`
    - `xmake config -p linux` succeeds
    - `xmake build SkyrimCoopNative` produces a binary
  </acceptance_criteria>
  <done>Native client XMake target builds as a Linux ELF binary linking EnTT, spdlog, encoding, and server natively. Root xmake.lua includes both new targets. Both targets coexist without conflicting with existing build.</done>
</task>

</tasks>

<verification>
- `xmake config -p mingw -y && xmake build SkyrimCoopHooksDLL` — DLL target builds
- `xmake config -p linux -y && xmake build SkyrimCoopNative` — native target builds
- Neither new target breaks existing targets (`xmake build SkyrimTogetherClient` still works on mingw)
</verification>

<success_criteria>
Both new XMake targets (MinGW relay DLL and native Linux ELF) configure and build successfully, with correct dependency sets per D-12 and D-13.
</success_criteria>

<output>
After completion, create `.planning/phases/07-native-linux-build-with-skse-tcp-relay/07-02-SUMMARY.md`
</output>
