---
phase: 03.1
slug: build-performance-resource-optimization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-27
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Catch2 2.13.9 (existing TPTests) + build-output verification |
| **Config file** | Code/tests/ (test files compiled by xmake target TPTests) |
| **Quick run command** | `xmake f -p mingw -m devbuild && xmake -y` |
| **Full suite command** | `xmake f -p mingw -m devbuild && xmake -y && xmake f -p mingw -m releasedbg && xmake -y && xmake build TPTests && xmake run TPTests` |
| **Estimated runtime** | ~120 seconds (full suite with both modes) |

---

## Sampling Rate

- **After every task commit:** Run `xmake f -p mingw -m devbuild && xmake -y`
- **After every plan wave:** Run full suite (both devbuild and releasedbg modes)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Behavior | Test Type | Automated Command | Notes | Status |
|----------|-----------|-------------------|-------|--------|
| devbuild mode produces working DLL | smoke | `xmake f -p mingw -m devbuild && xmake -y && file build/*/SkyrimTogetherClient.dll` | DLL must be valid PE | ⬜ pending |
| ccache accelerates rebuild | manual | `xmake -r && time xmake -y` (second build should be faster) | Measure time delta | ⬜ pending |
| lld used as linker | smoke | `xmake -v 2>&1 \| grep lld` | Should see -fuse-ld=lld | ⬜ pending |
| split DWARF produces .dwo files | smoke | `find build/ -name "*.dwo" \| head -5` | .dwo files must exist | ⬜ pending |
| shared internal libs produced | smoke | `find build/ -name "*.dll" \| grep -v SkyrimTogether` | Internal DLLs must exist | ⬜ pending |
| releasedbg mode unchanged | regression | `xmake f -p mingw -m releasedbg && xmake -y` | Must still produce working DLL | ⬜ pending |
| TPTests still pass | unit | `xmake build TPTests && xmake run TPTests` | All existing tests pass | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements. Validation is build-output-focused, not unit-test-focused.*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| ccache accelerates rebuild | Requires timing comparison between first and second build | Build twice, compare wall-clock times |
| Build time improvement measurable | Requires baseline comparison | Time full build before and after phase, compare |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
