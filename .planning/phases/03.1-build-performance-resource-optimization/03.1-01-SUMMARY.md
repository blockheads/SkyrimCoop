---
phase: 03.1-build-performance-resource-optimization
plan: 01
subsystem: infra
tags: [xmake, lld, split-dwarf, ccache, mingw, devbuild, cross-compile]

# Dependency graph
requires:
  - phase: 03-client-dll-cross-compilation
    provides: MinGW cross-compilation toolchain and client DLL build
provides:
  - mode.devbuild XMake rule with -O0, debug symbols, no NDEBUG
  - lld linker integration for 2-5x faster PE/COFF linking
  - split DWARF (-gsplit-dwarf) for separate .dwo debug info files
  - ccache enabled conditionally for devbuild mode
  - unity build batchsize tuned to 4 for devbuild incremental builds
  - lld symlink in .toolchain/bin/ for MinGW GCC resolution
affects: [03.1-02, 03.1-03, build-performance]

# Tech tracking
tech-stack:
  added: [lld-18, ccache, split-dwarf]
  patterns: [devbuild-mode-conditional-flags, toolchain-local-symlinks]

key-files:
  created:
    - .toolchain/bin/ld.lld
  modified:
    - xmake.lua

key-decisions:
  - "lld over mold: mold cannot produce PE/COFF output required for Windows DLL cross-compilation"
  - "Blanket -static removed in devbuild mode to prepare for shared internal libs in Plan 02"
  - "Unity batchsize=4 in devbuild for fewer recompiles on single-file changes"

patterns-established:
  - "is_mode('devbuild') guard: all devbuild-specific flags wrapped in mode check, non-devbuild unchanged"
  - "Toolchain-local symlinks: .toolchain/bin/ contains project-specific symlinks to system tools"

requirements-completed: [D-01, D-02, D-04, D-06, D-07]

# Metrics
duration: 2min
completed: 2026-03-28
---

# Phase 03.1 Plan 01: Devbuild Mode Summary

**XMake devbuild mode with lld linker, split DWARF, ccache, and tuned unity batchsize for fast MinGW cross-compile iteration**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-28T03:48:01Z
- **Completed:** 2026-03-28T03:50:30Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Created lld symlink in .toolchain/bin/ enabling -fuse-ld=lld for MinGW GCC cross-compiler
- Added mode.devbuild rule with -O0, debug symbols, assertions, lld, split DWARF, ccache, and tuned unity batchsize
- Non-devbuild modes (debug, releasedbg, release) completely unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: Create lld symlink in toolchain directory** - `91d19526` (chore)
2. **Task 2: Add mode.devbuild rule with lld, split DWARF, ccache, and auto-detect** - `4295fcbd` (feat)

## Files Created/Modified
- `.toolchain/bin/ld.lld` - Symlink to /usr/bin/ld.lld-18 for -fuse-ld=lld resolution
- `xmake.lua` - mode.devbuild rule, conditional ccache, lld/split-DWARF flags, tuned unity batchsize

## Decisions Made
- Used lld instead of mold because mold cannot produce PE/COFF output needed for Windows DLL cross-compilation
- Removed blanket -static flag in devbuild mode to prepare for shared internal libs (Plan 02)
- Unity batchsize reduced to 4 in devbuild for fewer recompiles on single-file changes vs 12 for CI/release
- Added -B flag approach for GCC to find lld symlink via .toolchain/bin/ PATH

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- devbuild mode is defined and ready; `xmake f -p mingw -m devbuild` configuration validation is in Plan 03
- Plan 02 can now build on devbuild mode to add shared library splitting
- lld symlink requires lld-18 installed on the system (`apt install lld-18`)

---
*Phase: 03.1-build-performance-resource-optimization*
*Completed: 2026-03-28*
